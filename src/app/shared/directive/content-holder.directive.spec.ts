import { ContentHolderDirective } from './content-holder.directive';

describe('ContentHolderDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentHolderDirective();
    expect(directive).toBeTruthy();
  });
});
